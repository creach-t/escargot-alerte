# Configuration du serveur
PORT=5000

# API Météo (ex: OpenWeatherMap)
WEATHER_API_KEY=votre_clé_api_météo

# MongoDB (optionnel pour le MVP)
MONGODB_URI=mongodb://localhost:27017/escargot-alerte

# JWT pour l'authentification (optionnel pour le MVP)
JWT_SECRET=votre_secret_jwt

# Configuration des notifications push (optionnel)
PUSH_NOTIFICATION_KEY=votre_clé_api_notifications
